import React from "react";
import { Container, Panel } from "rsuite";

const Dashboard = () => (
  <Container>
    <Panel header="Панель управління" bordered>
      Тут буде контент адмінпанелі.
    </Panel>
  </Container>
);

export default Dashboard;
